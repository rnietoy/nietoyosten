@{
    ViewBag.Title = "Recover password";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Recuperación de contraseña</h2>
Por favor escribe la dirección de correo con el que estás dado de alta para enviarte un nuevo password.

@using (Html.BeginForm("RecoverPassword", "Account", FormMethod.Post, new { role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <div class="row">
        <div class="col-md-5">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" class="form-control" name="email" id="email" data-val="true" data-val-required="Requerido">
                <span class="field-validation-valid" data-valmsg-for="reason" data-valmsg-replace="true"></span>
            </div>
        </div>
    </div>
    <button type="submit" class="btn btn-default">Enviar</button>
}
